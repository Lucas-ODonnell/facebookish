<% if current_user.liked? @post %>
    $('.like-btn').addClass('liked');
<% else %>
    $('.like-btn').addClass('like-btn');
<% end %>
  <%# This specifies the post we want changed. Same for the comment version %>
  $('.likes-count-<%= @post.id %>').html("<%= @post.get_likes.size %>")

